# Static site generator (extended build: SCSS, WebP, etc.)
brew "hugo"
# JavaScript runtime + package manager — runs Prettier and Biome via bunx
brew "bun"
# Task runner (Taskfile.yml)
brew "go-task"
# Image processing for site assets (magick, potrace)
brew "imagemagick"
brew "potrace"
